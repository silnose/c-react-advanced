import styled from 'styled-components'

export const Button = styled.button`
  border: none;
  padding-top: 8px;
  background-color: transparent;
  display: flex;
  align-items: center;
  outline: transparent;
  & svg {
    margin-right: 4px;
    cursor: pointer;
  }
`
